# 可扩展性计划

## 水平扩展
- API 层以无状态服务部署，多实例负载均衡。
- 登录状态建议使用集中式会话存储或 token。

## API 网关
- 统一鉴权、限流、日志、监控。
- 对外只暴露网关入口，隐藏内部服务。

## 服务拆分（可选）
- `auth`：认证与用户管理
- `order`：订单管理
- `upload`：图片与文件服务
拆分后可独立扩容与优化。

## 数据库扩展
- 读写分离与只读副本。
- 订单表按时间分区或按用户分片。
- 热数据缓存（Redis）。

## 图片存储
- 迁移到对象存储（S3/OSS）。
- 接入 CDN 进行缓存与加速。
