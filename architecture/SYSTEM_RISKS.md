# 系统风险

## 安全风险
- 认证仅依赖前端状态，后端若未校验会存在越权风险。
- 上传接口可能被恶意利用（文件类型、大小、路径穿越）。

## 数据一致性风险
- 订单状态更新与消息/图片上传存在并发写入风险。
- 若缺少事务处理，可能导致时间线不一致。

## 文件上传风险
- 文件存储与数据库记录不一致（上传成功但记录失败）。
- 缺少清理机制导致存储膨胀。

## API 滥用风险
- 缺少限流与鉴权拦截。
- 管理端接口若暴露，风险更高。

## 运维风险
- 单机部署存在单点故障。
- 缺少监控与日志聚合会影响故障排查。
